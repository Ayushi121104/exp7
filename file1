#include <stdio.h>
int main() {    

    int a, b, sum;
    
    printf("Enter the value of two integers: ");
    scanf("%d %d", &a, &b);

    // calculate the sum of a and b
    sum = a+ b;      
    
    printf("%d + %d = %d", a, b, sum);
    return 0;
}

